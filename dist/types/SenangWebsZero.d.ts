import type { SWZOptions, SWZStep, InternalStep } from './types';
type LifecycleHandler = () => void | Promise<unknown>;
export declare class SenangWebsZero {
    isVisible: boolean;
    activeStep: number;
    tourSteps: SWZStep[];
    backendSteps: InternalStep[];
    dialog: HTMLElement;
    backdrop: HTMLElement;
    options: SWZOptions;
    group?: string;
    private _objectSteps;
    private _onBeforeStepChange;
    private _onAfterStepChange;
    private _onBeforeExit;
    private _onAfterExit;
    private _onFinish;
    private _currentGroup;
    private _resizeDebounceTimer;
    private _keyHandler;
    private _backdropClickHandler;
    private _dotClickHandler;
    private _previousFocus;
    constructor(userOptions?: SWZOptions);
    start(group?: string): Promise<void>;
    visitStep(step: 'next' | 'prev' | number): Promise<void>;
    nextStep(): Promise<void>;
    prevStep(): Promise<void>;
    exit(): Promise<void>;
    finishTour(exit?: boolean, group?: string): Promise<void>;
    isFinishedMethod(group?: string): boolean;
    deleteFinishedTour(group?: string): void;
    addSteps(steps: SWZStep[]): void;
    setOptions(userOptions: SWZOptions): void;
    refresh(): Promise<void>;
    refreshDialog(): Promise<void>;
    updatePositions(): Promise<void>;
    onBeforeStepChange(fn: LifecycleHandler): void;
    onAfterStepChange(fn: LifecycleHandler): void;
    onBeforeExit(fn: LifecycleHandler): void;
    onAfterExit(fn: LifecycleHandler): void;
    onFinish(fn: LifecycleHandler): void;
    finish(exit?: boolean, group?: string): Promise<void>;
    isFinished(group?: string): boolean;
    private _resolveSteps;
    private _mount;
    private _teardown;
    private _goToStep;
    private _renderActiveStep;
    private _positionDialog;
    private _finish;
    private _trapTab;
    private _onResize;
    private _replaceObjectSteps;
    private _syncPublicStepsToBackend;
    private _setAnimationClasses;
    private _restoreFocus;
}
export {};
